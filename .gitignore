*.log
.DS_Store
.env
.claude/
.env.backup
.env.production
.env.testing
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
Homestead.json
Homestead.yaml
Thumbs.db

# Phase -1: backups and staging artifacts
/storage/app/backup-prod-*.sql
/storage/app/backup-prod-*.sql.gz
# Sanitised fixture: plaintext SQL must never be committed; only the .enc version is safe
/tests/fixtures/sanitised-snapshot.sql

# Phase 1: GitNexus — index db and generated artifacts (not source)
.gitnexus
/storage/gitnexus/
/tools/gitnexus/

# Phase 2: Python sidecar artifacts (sidecar/.gitignore handles the rest)
/sidecar/__pycache__/
/sidecar/.venv/
